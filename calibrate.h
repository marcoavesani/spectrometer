#ifndef CALIBRATE_H
#define CALIBRATE_H

#include <QWidget>

class calibrate : public QWidget
{
    Q_OBJECT
public:
    explicit calibrate(QWidget *parent = 0);

signals:

public slots:
};

#endif // CALIBRATE_H